# Only what the current phases use. Add a dependency when the phase that needs
# it starts, not before — see ROADMAP.md.

# app + config
streamlit>=1.35
pyyaml>=6.0
python-dotenv>=1.0
pandas>=2.2

# ask / chat
anthropic>=0.40

# charts
plotly>=5.20

# Phase 3 (journal): edgartools        — filings retrieval
# Phase 4 (scan):    yfinance          — prices and basic fundamentals
# Phase 4 (scan):    financetoolkit    — ratios and valuation models
# Phase 4 (scan):    ta or pandas-ta-classic — indicators. NB: `pandas-ta` is
#   unmaintained and no longer installable; pick a live fork when we get there.
# Phase 6 (audio):   edge-tts or piper-tts
